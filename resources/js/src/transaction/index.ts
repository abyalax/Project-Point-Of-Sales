import TransactionManager from "./module/transaction-manager"
import TransactionUI from "./module/transaction-ui";

document.addEventListener('DOMContentLoaded', () => {
    new TransactionManager()
   new TransactionUI()
})